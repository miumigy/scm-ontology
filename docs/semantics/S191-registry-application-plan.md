# S191 — Registry Application Plan

S191 introduces an explicit plan artifact between an accepted extension proposal and any future canonical registry mutation.

```text
ACCEPTED ExtensionProposal
          ↓
RegistryApplicationPlan
          ↓
[future governed application]
```

The plan is immutable and observational. Creating a plan does not mutate the canonical registry, relation registry, graph, or ontology.
