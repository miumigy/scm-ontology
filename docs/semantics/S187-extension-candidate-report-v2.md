# S187 — Extension Candidate Report

S187 packages extension candidates into an immutable report artifact.

The report is a read-only governance input. It does not approve candidates, mutate the ontology, infer new relations, or modify the graph.
