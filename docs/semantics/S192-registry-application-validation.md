# S192 — Registry Application Plan Validation

S192 validates the structural completeness of a registry application plan before any future registry mutation.

Required fields:
- candidate reference
- predicate reference
- subject type
- object type

This is structural validation only. It does not check semantic truth, mutate the canonical registry, mutate the graph, or apply an ontology extension.
