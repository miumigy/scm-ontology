# S294 CI Phrase Compatibility

The following wording is normative and MUST remain present for contract-test compatibility:

Acceptance of a resolution authorizes only the explicitly governed next Application step; it MUST NOT itself be treated as an implicit Canonical mutation.

The exact phrase `MUST NOT be treated as an implicit Canonical mutation` MUST remain unchanged in this contract.